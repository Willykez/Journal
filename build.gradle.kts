// Top-level build file. If Android Studio prompts you to upgrade the
// Android Gradle Plugin or Kotlin version when you open this project,
// accept the upgrade — these are pinned to versions known-good as of
// late 2025/early 2026 but newer patch versions are fine.
plugins {
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
